# فعال‌سازی همیشگی هوش مصنوعی (بدون وارد کردن دستی کلید)

هدف: کسی که برنامه رو ازت می‌گیره، هیچ‌وقت کلید API نمی‌بینه و هوش مصنوعی
از همون اول کار می‌کنه.

## چرا نمی‌شه کلید Groq رو مستقیم توی app.py گذاشت؟
چون app.py یه فایل پایتون ساده و قابل‌خوندنه. هر کسی که برنامه رو نصب کنه
می‌تونه فایل رو باز کنه، کلید رو ببینه و ازش سوءاستفاده کنه (حساب Groq تو
پر از هزینه می‌شه). راه‌حل درست اینه که کلید واقعی فقط روی سروری بمونه که
خودت کنترلش می‌کنی.

## مراحل

### ۱. دیپلوی پروکسی (پوشه `ai_proxy/`)
این یه سرور کوچیک جداست که کلید واقعی Groq رو نگه می‌داره و درخواست‌ها رو
براش می‌فرسته. هرجایی که پایتون اجرا می‌شه (یه سرور ۵ دلاری، Render،
Railway، PythonAnywhere و…) قابل اجراست:

```bash
cd ai_proxy
pip install flask
GROQ_API_KEY=کلید_واقعی_Groq_تو PROXY_TOKEN=یه-رشته-تصادفی-طولانی python3 ai_proxy.py
```

`PROXY_TOKEN` کلید Groq نیست — فقط یه رمز دلخواهه که خودت می‌سازی تا هرکسی
که آدرس پروکسی رو پیدا کرد نتونه ازش استفاده کنه.

### ۲. تنظیم اپ اصلی Dijo
روی سروری که خود برنامه Dijo رو اجرا می‌کنی (یا قبل از پکیج کردن برای
توزیع)، این دو متغیر محیطی رو ست کن:

```bash
export DIJO_AI_ENDPOINT="https://آدرس-پروکسی-تو/v1/chat/completions"
export DIJO_AI_API_KEY="همون PROXY_TOKEN بالا"
```

همین. کاربر نهایی هیچ صفحه‌ای برای وارد کردن کلید نمی‌بینه؛ چت دیجو از
همون اولین اجرا فعاله.

### نکته امنیتی
این یه مکانیزم محافظتی ساده‌ست (توکن مشترک)، نه احراز هویت پیشرفته. اگه
قراره برنامه رو به تعداد زیاد بفروشی/پخش کنی، بعداً باید محدودیت نرخ
درخواست (rate limit) هم به پروکسی اضافه کنی تا اگه یه نسخه لو رفت، کل
هزینه Groq تو بالا نره.

### حالت آفلاین
اگه دستگاهی به اینترنت وصل نباشه، پروکسی هم در دسترس نیست، پس چت هوش
مصنوعی جواب نمی‌ده — ولی بقیهٔ برنامه (فروش، انبار، مشتریان، گزارش‌ها)
چون روی SQLite محلیه، کاملاً آفلاین کار می‌کنه.
